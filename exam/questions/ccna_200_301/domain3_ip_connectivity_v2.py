"""Domain 3 — IP Connectivity v2 (Cisco CCNA 200-301).

Second bank of 40 scenario-based questions for Domain 3 (25% of exam).
Covers objectives 3.1–3.5 with fresh scenarios, misconception-targeting
distractors, and verified routing math. IDs cd3v2-001 through cd3v2-040.
"""

QUESTIONS = [
    # ------------------------------------------------- 3.1 routing table (6 Qs)
    {
        "id": "cd3v2-001",
        "domain": 3,
        "objective": "3.1",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "Routing table components",
        "stem": (
            "A 'show ip route' on R1 displays:\n"
            "  D    172.16.0.0/16 is variably subnetted, 3 subnets, 2 masks\n"
            "  D      172.16.1.0/24 [90/3072] via 10.0.0.2\n"
            "  D      172.16.2.0/24 [90/5120] via 10.0.0.6\n"
            "  D      172.16.0.0/16 [90/7680] via 10.0.0.10\n"
            "A packet arrives destined for 172.16.3.100. Which route is selected?"
        ),
        "options": [
            {
                "id": "a",
                "text": "172.16.0.0/16 via 10.0.0.10",
                "correct": True,
                "rationale": (
                    "Correct. 172.16.3.100 does NOT fall in 172.16.1.0/24 (.1.0–.1.255) "
                    "nor 172.16.2.0/24 (.2.0–.2.255). It IS within 172.16.0.0/16 (.0.0–.255.255), "
                    "which is the only matching route, so it is selected regardless of its higher metric."
                ),
            },
            {
                "id": "b",
                "text": "172.16.1.0/24 via 10.0.0.2 (lowest metric 3072)",
                "correct": False,
                "rationale": (
                    "Incorrect. 172.16.3.100 is outside 172.16.1.0/24 (.1.0–.1.255). "
                    "A route must CONTAIN the destination before metric is considered; "
                    "this /24 does not contain .3.100."
                ),
            },
            {
                "id": "c",
                "text": "172.16.2.0/24 via 10.0.0.6",
                "correct": False,
                "rationale": (
                    "Incorrect. 172.16.3.100 is not in 172.16.2.0/24 (.2.0–.2.255). "
                    "Only the /16 summary contains the destination."
                ),
            },
            {
                "id": "d",
                "text": "Packet is dropped; no route exists for 172.16.3.x",
                "correct": False,
                "rationale": (
                    "Incorrect. The summary route 172.16.0.0/16 covers all 172.16.x.x addresses, "
                    "including 172.16.3.100, so the packet is forwarded, not dropped."
                ),
            },
        ],
        "explanation": (
            "Longest-prefix match first selects all routes that CONTAIN the destination. "
            "172.16.3.100 matches only the /16 summary (not the /24s), so the /16 wins. "
            "Metric matters only after a matching route is identified."
        ),
    },
    {
        "id": "cd3v2-002",
        "domain": 3,
        "objective": "3.1",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "Routing table components",
        "stem": (
            "A router's table shows:\n"
            "  O IA 10.20.0.0/22 [110/30] via 192.168.0.1\n"
            "What does the 'IA' suffix on the 'O' route code indicate?"
        ),
        "options": [
            {
                "id": "a",
                "text": "The route was learned from another OSPF area (inter-area), originating from an ABR",
                "correct": True,
                "rationale": (
                    "Correct. 'O IA' means OSPF Inter-Area route. It was generated by an "
                    "Area Border Router (ABR) as a Type 3 LSA, summarizing networks from "
                    "a different area into the local area."
                ),
            },
            {
                "id": "b",
                "text": "The route was injected into OSPF from an external source (redistribution)",
                "correct": False,
                "rationale": (
                    "Incorrect. Redistributed external routes appear as 'O E1' or 'O E2', "
                    "not 'O IA'. Inter-area is different from external."
                ),
            },
            {
                "id": "c",
                "text": "The route is an OSPF internal autonomous-system route",
                "correct": False,
                "rationale": (
                    "Incorrect. 'IA' stands for Inter-Area, not internal autonomous system. "
                    "Intra-area routes are simply coded 'O'."
                ),
            },
            {
                "id": "d",
                "text": "The route was imported via an import policy on an ASBR",
                "correct": False,
                "rationale": (
                    "Incorrect. ASBR-injected routes become 'O E1' or 'O E2'; 'IA' "
                    "specifically means inter-area, from an ABR."
                ),
            },
        ],
        "explanation": (
            "OSPF route codes: 'O' intra-area, 'O IA' inter-area (Type 3 LSA, from ABR), "
            "'O E1'/'O E2' external (Type 5/7 LSA, from ASBR). Inter-area routes have "
            "default AD 110, same as intra-area."
        ),
    },
    {
        "id": "cd3v2-003",
        "domain": 3,
        "objective": "3.1",
        "type": "multiple_choice",
        "difficulty": "expert",
        "study_topic": "Routing table components",
        "stem": (
            "The routing table shows:\n"
            "  D EX 192.0.2.0/24 [170/20514560] via 10.1.1.2\n"
            "What does the 'D EX' code indicate, and why is the AD 170 significant?"
        ),
        "options": [
            {
                "id": "a",
                "text": (
                    "'D EX' is an EIGRP external route, redistributed into EIGRP from another source; "
                    "AD 170 is less trusted than internal EIGRP (90) to discourage preferring redistribution paths"
                ),
                "correct": True,
                "rationale": (
                    "Correct. 'D' is EIGRP, 'EX' means external (redistributed from outside EIGRP). "
                    "EIGRP assigns these AD 170 vs. internal 90 so that a true internal EIGRP path "
                    "is preferred over a redistributed one for the same prefix, preventing suboptimal routing."
                ),
            },
            {
                "id": "b",
                "text": "'D EX' means the route was learned from an OSPF external area and has no relation to EIGRP",
                "correct": False,
                "rationale": (
                    "Incorrect. 'D' is EIGRP's code. OSPF external routes use 'O E1'/'O E2'. "
                    "'D EX' specifically means redistributed-into-EIGRP external."
                ),
            },
            {
                "id": "c",
                "text": "AD 170 is the default EIGRP internal distance; external routes use AD 90",
                "correct": False,
                "rationale": (
                    "Incorrect. AD values are reversed: EIGRP internal = 90, EIGRP external = 170. "
                    "Higher AD is less preferred."
                ),
            },
            {
                "id": "d",
                "text": "The route is a directly connected network tagged by EIGRP",
                "correct": False,
                "rationale": (
                    "Incorrect. Connected routes are code 'C' with AD 0. 'D EX' means an "
                    "external redistributed route, not a directly connected one."
                ),
            },
        ],
        "explanation": (
            "EIGRP internal routes (AD 90) are routes native to the EIGRP domain. "
            "EIGRP external routes (D EX, AD 170) have been redistributed from another protocol. "
            "The higher AD ensures an internal EIGRP path is always preferred over a redistribution "
            "path for the same prefix, limiting routing loops."
        ),
    },
    {
        "id": "cd3v2-004",
        "domain": 3,
        "objective": "3.1",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "Routing table components",
        "stem": (
            "After adding a new loopback interface (Lo0: 10.99.99.1/32) to a router running OSPF, "
            "an engineer issues 'show ip route 10.99.99.1'. The table shows BOTH:\n"
            "  C  10.99.99.1/32 is directly connected, Loopback0\n"
            "  L  10.99.99.1/32 is directly connected, Loopback0\n"
            "Why are there TWO entries for the same address?"
        ),
        "options": [
            {
                "id": "a",
                "text": (
                    "'C' is the connected network route (the /32 subnet) and 'L' is the local "
                    "host route explicitly for this router's own address; both are auto-created "
                    "for each configured interface IP in IOS"
                ),
                "correct": True,
                "rationale": (
                    "Correct. For every interface IP, IOS automatically installs a 'C' route for "
                    "the subnet and an 'L' (local) /32 route for the router's own address. "
                    "For a loopback with a /32 mask, both entries are identical in prefix but "
                    "serve different purposes: 'C' represents the reachable network, "
                    "'L' is used for locally-destined traffic."
                ),
            },
            {
                "id": "b",
                "text": "There is a routing loop; the route should appear only once",
                "correct": False,
                "rationale": (
                    "Incorrect. Dual 'C' and 'L' entries are normal IOS behavior since IOS 15+; "
                    "they do not indicate a loop."
                ),
            },
            {
                "id": "c",
                "text": "'L' means the route was learned via OSPF and 'C' is the static route",
                "correct": False,
                "rationale": (
                    "Incorrect. 'L' is local (auto-generated host route for the router's own IP); "
                    "OSPF routes are 'O'. 'C' is connected, not static ('S')."
                ),
            },
            {
                "id": "d",
                "text": "The duplicate is caused by OSPF advertising the loopback back to itself",
                "correct": False,
                "rationale": (
                    "Incorrect. These entries come from the directly connected interface, "
                    "not from OSPF. They appear as soon as the interface is configured, "
                    "regardless of OSPF."
                ),
            },
        ],
        "explanation": (
            "IOS creates two routing table entries per interface: 'C' for the subnet "
            "and 'L' for the router's own IP as a /32, allowing the router to differentiate "
            "traffic destined for the subnet from traffic destined for itself. "
            "This is standard IOS 15+ behavior."
        ),
    },
    {
        "id": "cd3v2-005",
        "domain": 3,
        "objective": "3.1",
        "type": "multiple_response",
        "difficulty": "expert",
        "study_topic": "Routing table components",
        "stem": (
            "Which TWO pieces of information are encoded in the routing-table entry "
            "'O 10.100.0.0/22 [110/20] via 10.0.12.2, 00:03:41, GigabitEthernet0/1'? (Choose TWO.)"
        ),
        "options": [
            {
                "id": "a",
                "text": "The route was learned via OSPF with an administrative distance of 110",
                "correct": True,
                "rationale": (
                    "Correct. The 'O' code indicates OSPF and the first bracket value (110) "
                    "is the administrative distance."
                ),
            },
            {
                "id": "b",
                "text": "The OSPF cost (metric) to 10.100.0.0/22 is 20",
                "correct": True,
                "rationale": (
                    "Correct. The second bracket value (20) is the OSPF metric (cumulative cost). "
                    "AD/metric are encoded as [AD/metric] in the routing table."
                ),
            },
            {
                "id": "c",
                "text": "The route has been in the table for 3 hours and 41 minutes",
                "correct": False,
                "rationale": (
                    "Incorrect. The timestamp 00:03:41 is in HH:MM:SS format, meaning "
                    "3 minutes and 41 seconds, not 3 hours."
                ),
            },
            {
                "id": "d",
                "text": "110 is the OSPF process ID on the remote router",
                "correct": False,
                "rationale": (
                    "Incorrect. 110 is the administrative distance of OSPF, not a process ID. "
                    "Process IDs are locally significant and do not appear in routing entries."
                ),
            },
        ],
        "explanation": (
            "Routing table format: <code> <prefix> [AD/metric] via <next-hop>, <age>, <interface>. "
            "'O' = OSPF, [110/20] = [AD/OSPF-cost], 00:03:41 = 3 min 41 sec since last update."
        ),
    },
    {
        "id": "cd3v2-006",
        "domain": 3,
        "objective": "3.1",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "Routing table components",
        "stem": (
            "R1 has a route '10.50.0.0/16 is possibly down, routing via 10.0.0.5'. "
            "What does 'possibly down' mean in this context?"
        ),
        "options": [
            {
                "id": "a",
                "text": (
                    "The outgoing interface or next-hop associated with the route is down; "
                    "the route is retained but marked suspect until it is removed or the interface recovers"
                ),
                "correct": True,
                "rationale": (
                    "Correct. 'Possibly down' appears when a route's interface goes down but the "
                    "route hasn't yet been purged (e.g., during holddown). The router flags it as "
                    "unreliable rather than removing it immediately."
                ),
            },
            {
                "id": "b",
                "text": "The prefix 10.50.0.0/16 has never been reachable",
                "correct": False,
                "rationale": (
                    "Incorrect. 'Possibly down' means it WAS known and is now suspect; "
                    "it's not the same as never having been reachable."
                ),
            },
            {
                "id": "c",
                "text": "The route has a higher AD than another route to the same prefix",
                "correct": False,
                "rationale": (
                    "Incorrect. AD comparison never produces a 'possibly down' marking; "
                    "that label is about interface/next-hop reachability."
                ),
            },
            {
                "id": "d",
                "text": "OSPF is recalculating SPF and the route is temporarily unavailable",
                "correct": False,
                "rationale": (
                    "Incorrect. SPF recalculation doesn't label routes 'possibly down'; "
                    "the label indicates the forwarding path (interface/next hop) is down."
                ),
            },
        ],
        "explanation": (
            "'Possibly down' in a routing table entry means the associated interface or next-hop "
            "is unreachable, making the route suspect. The router may retain it briefly before "
            "purging it. This often appears during convergence or when an interface fails."
        ),
    },
    # ------------------------------------------- 3.2 forwarding / LPM / AD (8 Qs)
    {
        "id": "cd3v2-007",
        "domain": 3,
        "objective": "3.2",
        "type": "multiple_choice",
        "difficulty": "expert",
        "study_topic": "Forwarding decision & longest prefix match",
        "stem": (
            "R1's table has:\n"
            "  S    10.0.0.0/8    via 172.16.0.1\n"
            "  O    10.10.0.0/16  via 172.16.0.2\n"
            "  D    10.10.10.0/24 via 172.16.0.3\n"
            "  C    10.10.10.0/25 (GigabitEthernet0/0)\n"
            "A packet arrives for 10.10.10.50. Which route is used?"
        ),
        "options": [
            {
                "id": "a",
                "text": "10.10.10.0/25 via GigabitEthernet0/0 (directly connected, longest match)",
                "correct": True,
                "rationale": (
                    "Correct. 10.10.10.50 in binary: .50 = 0011 0010. The /25 covers .0–.127 "
                    "(high bit = 0), so .50 is within 10.10.10.0/25. Among all matching routes "
                    "(/8, /16, /24, /25), the /25 is longest. Connected (AD 0) also helps, "
                    "but longest prefix is determined first."
                ),
            },
            {
                "id": "b",
                "text": "10.10.10.0/24 via 172.16.0.3 (EIGRP, because EIGRP beats OSPF)",
                "correct": False,
                "rationale": (
                    "Incorrect. While EIGRP (90) beats OSPF (110) by AD, AD is compared only "
                    "between routes of the SAME prefix length. The /25 is more specific than "
                    "the /24 and contains .50, so the /25 wins outright."
                ),
            },
            {
                "id": "c",
                "text": "10.0.0.0/8 via 172.16.0.1 (static, lowest AD of 1)",
                "correct": False,
                "rationale": (
                    "Incorrect. AD 1 for the static /8 is irrelevant; the /8 is the LEAST "
                    "specific matching route. Longest prefix always wins regardless of AD."
                ),
            },
            {
                "id": "d",
                "text": "10.10.0.0/16 via 172.16.0.2 (OSPF)",
                "correct": False,
                "rationale": (
                    "Incorrect. The /16 contains .50 but is less specific than both the /24 and /25. "
                    "The /25 is the longest matching prefix."
                ),
            },
        ],
        "explanation": (
            "Forwarding decision: longest prefix match first, then AD, then metric. "
            "10.10.10.50 fits inside /8, /16, /24, and /25; the /25 is longest (most specific) "
            "and wins unconditionally. Connected (C) at /25 is selected."
        ),
    },
    {
        "id": "cd3v2-008",
        "domain": 3,
        "objective": "3.2",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "Administrative distance",
        "stem": (
            "A router has two routes to 192.168.100.0/24:\n"
            "  - From RIP with metric 3 (AD 120)\n"
            "  - From OSPF with metric 1000 (AD 110)\n"
            "Which route is installed, and why is this counter-intuitive?"
        ),
        "options": [
            {
                "id": "a",
                "text": (
                    "OSPF (AD 110) is installed. Even though its metric (1000) is far worse than "
                    "RIP's (3), AD compares trustworthiness across protocols FIRST; metric is only "
                    "relevant within a single protocol"
                ),
                "correct": True,
                "rationale": (
                    "Correct. AD is evaluated before metric when different protocols offer the same "
                    "prefix. OSPF wins (AD 110 < RIP 120) despite having a much worse metric. "
                    "Metric (hop count or cost) is only compared among routes from the same protocol."
                ),
            },
            {
                "id": "b",
                "text": "RIP (metric 3) is installed because lower metric always wins",
                "correct": False,
                "rationale": (
                    "Incorrect. Metric is NOT compared across different protocols. "
                    "AD arbitrates between protocols: OSPF (110) beats RIP (120) first."
                ),
            },
            {
                "id": "c",
                "text": "Both routes are installed and traffic is split proportionally",
                "correct": False,
                "rationale": (
                    "Incorrect. Different protocols for the same prefix do not load-balance; "
                    "the lower-AD protocol's route is installed and the other is not."
                ),
            },
            {
                "id": "d",
                "text": "The route with the higher metric (OSPF, 1000) is discarded automatically",
                "correct": False,
                "rationale": (
                    "Incorrect. Metric comparison between different protocols is meaningless; "
                    "the selection is by AD. OSPF's metric of 1000 is an OSPF-internal cost "
                    "and cannot be compared to RIP's hop count."
                ),
            },
        ],
        "explanation": (
            "AD resolves protocol disagreements for the same prefix: lower AD = installed. "
            "Metric is protocol-specific and is only compared among routes FROM THE SAME PROTOCOL "
            "for the same prefix. RIP hop counts and OSPF costs are apples-and-oranges."
        ),
    },
    {
        "id": "cd3v2-009",
        "domain": 3,
        "objective": "3.2",
        "type": "multiple_choice",
        "difficulty": "expert",
        "study_topic": "Forwarding decision & longest prefix match",
        "stem": (
            "A router must forward a packet to 172.31.255.200. The table contains ONLY:\n"
            "  172.31.254.0/23 via A  (covers 172.31.254.0–172.31.255.255)\n"
            "  172.31.255.0/24 via B  (covers 172.31.255.0–172.31.255.255)\n"
            "  172.31.255.192/26 via C (covers 172.31.255.192–172.31.255.255)\n"
            "  172.31.255.128/25 via D (covers 172.31.255.128–172.31.255.255)\n"
            "Which path is chosen?"
        ),
        "options": [
            {
                "id": "a",
                "text": "Via C — 172.31.255.192/26 is the longest matching prefix",
                "correct": True,
                "rationale": (
                    "Correct. 172.31.255.200 is within all four prefixes. Checking length: "
                    "/23 (shortest), /24, /25, /26 (longest). .200 is in .192/26 range (.192–.255), "
                    "so /26 is the longest prefix that contains it. C is chosen."
                ),
            },
            {
                "id": "b",
                "text": "Via D — 172.31.255.128/25 because it is more specific than /24",
                "correct": False,
                "rationale": (
                    "Incorrect. While /25 is more specific than /24, the /26 is even MORE specific "
                    "and also contains .200. The longest match (/26) always wins."
                ),
            },
            {
                "id": "c",
                "text": "Via A — the /23 is used because it was installed first",
                "correct": False,
                "rationale": (
                    "Incorrect. Installation order does not determine route selection. "
                    "Longest prefix match is always applied; /23 is the least specific."
                ),
            },
            {
                "id": "d",
                "text": "Via B — /24 because it exactly matches the third octet",
                "correct": False,
                "rationale": (
                    "Incorrect. 'Exactly matching an octet boundary' is not a routing concept. "
                    "Bit-level prefix length determines specificity; /26 > /24."
                ),
            },
        ],
        "explanation": (
            "172.31.255.200 in binary ends with ...11001000. "
            "The /26 prefix 172.31.255.192 = ...11000000 covers .192–.255, "
            "matching all 26 bits and containing .200. It is the longest match and wins."
        ),
    },
    {
        "id": "cd3v2-010",
        "domain": 3,
        "objective": "3.2",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "Administrative distance",
        "stem": (
            "An engineer manually sets 'distance 85' inside the OSPF process for all OSPF routes "
            "on a router that also runs EIGRP for the same prefixes. What changes?"
        ),
        "options": [
            {
                "id": "a",
                "text": (
                    "OSPF routes now win over EIGRP internal routes (default AD 90) for shared prefixes, "
                    "because 85 < 90"
                ),
                "correct": True,
                "rationale": (
                    "Correct. The 'distance' command under router ospf changes the local AD for "
                    "OSPF routes. At AD 85, OSPF is now more preferred than EIGRP internal (90) "
                    "for the same prefix, reversing the default behavior."
                ),
            },
            {
                "id": "b",
                "text": "The metric of all OSPF routes is set to 85",
                "correct": False,
                "rationale": (
                    "Incorrect. 'distance' changes ADMINISTRATIVE DISTANCE, not the OSPF metric (cost). "
                    "Changing cost uses 'ip ospf cost' or 'auto-cost reference-bandwidth'."
                ),
            },
            {
                "id": "c",
                "text": "EIGRP routes are removed from the table because OSPF is now lower",
                "correct": False,
                "rationale": (
                    "Incorrect. Only routes for the SAME prefix compete; for prefixes unique to "
                    "EIGRP or OSPF, both still install. The change only affects shared prefixes."
                ),
            },
            {
                "id": "d",
                "text": "Nothing; 'distance' applies only to BGP, not OSPF",
                "correct": False,
                "rationale": (
                    "Incorrect. The 'distance' command is supported under multiple routing protocols "
                    "including OSPF ('distance ospf' for more granularity) and changes their local AD."
                ),
            },
        ],
        "explanation": (
            "Administrative distance can be customized with the 'distance' command inside a "
            "routing process. Lowering OSPF's AD below EIGRP's 90 (e.g., to 85) makes OSPF "
            "routes preferred for shared prefixes. This is a local change with no protocol signaling."
        ),
    },
    {
        "id": "cd3v2-011",
        "domain": 3,
        "objective": "3.2",
        "type": "multiple_choice",
        "difficulty": "expert",
        "study_topic": "Administrative distance",
        "stem": (
            "Router R1 learns prefix 10.5.0.0/24 from three sources simultaneously:\n"
            "  - Static route (default AD 1)\n"
            "  - Connected interface (AD 0) — impossible here since it is a remote network\n"
            "  - EIGRP internal (AD 90)\n"
            "The prefix is NOT directly connected. Which route is installed?"
        ),
        "options": [
            {
                "id": "a",
                "text": "The static route (AD 1), because connected (AD 0) is impossible for a remote prefix",
                "correct": True,
                "rationale": (
                    "Correct. A connected route (AD 0) can only exist for an interface's own subnet "
                    "when that interface is up. Since 10.5.0.0/24 is a remote network, there is no "
                    "connected route for it. The static (AD 1) beats EIGRP (AD 90) and is installed."
                ),
            },
            {
                "id": "b",
                "text": "EIGRP (AD 90) because it has a dynamic metric",
                "correct": False,
                "rationale": (
                    "Incorrect. Static (AD 1) is lower than EIGRP (AD 90); the static route wins "
                    "regardless of EIGRP having a dynamic metric."
                ),
            },
            {
                "id": "c",
                "text": "Connected (AD 0) overrides everything, including for remote prefixes",
                "correct": False,
                "rationale": (
                    "Incorrect. A connected route (AD 0) only exists when an interface is directly "
                    "configured with that subnet. It cannot exist for a remote prefix."
                ),
            },
            {
                "id": "d",
                "text": "All three are installed in equal-cost load balancing",
                "correct": False,
                "rationale": (
                    "Incorrect. Only the lowest-AD source is installed for a given prefix; "
                    "they don't load-balance across different sources."
                ),
            },
        ],
        "explanation": (
            "For a remote (non-directly-connected) network, the connected route (AD 0) cannot exist. "
            "Among the real candidates—static (AD 1) and EIGRP (AD 90)—the static wins because "
            "1 < 90. AD 0 is exclusive to directly connected interfaces."
        ),
    },
    {
        "id": "cd3v2-012",
        "domain": 3,
        "objective": "3.2",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "Forwarding decision & longest prefix match",
        "stem": (
            "A router has only these two routes:\n"
            "  10.0.0.0/8    via X\n"
            "  10.0.0.0/24   via Y\n"
            "A packet arrives for 10.0.1.50. Which route is selected?"
        ),
        "options": [
            {
                "id": "a",
                "text": "10.0.0.0/8 via X — the /24 doesn't contain 10.0.1.50",
                "correct": True,
                "rationale": (
                    "Correct. 10.0.0.0/24 covers only 10.0.0.0–10.0.0.255. "
                    "10.0.1.50 is in the second subnet (10.0.1.x), which is outside the /24. "
                    "The only matching route is the /8 (10.0.0.0–10.255.255.255), so X is used."
                ),
            },
            {
                "id": "b",
                "text": "10.0.0.0/24 via Y — it is more specific",
                "correct": False,
                "rationale": (
                    "Incorrect. Specificity only applies to routes that CONTAIN the destination. "
                    "10.0.1.50 is NOT within 10.0.0.0/24, so the /24 doesn't match."
                ),
            },
            {
                "id": "c",
                "text": "Load-balanced across X and Y",
                "correct": False,
                "rationale": (
                    "Incorrect. These are different prefixes and the /24 doesn't even contain the "
                    "destination; there is only one matching route, so no balancing."
                ),
            },
            {
                "id": "d",
                "text": "Packet is dropped because there's no /32 host route",
                "correct": False,
                "rationale": (
                    "Incorrect. A host route (/32) is not required; the /8 matches and the packet "
                    "is forwarded to X."
                ),
            },
        ],
        "explanation": (
            "10.0.0.0/24 = 10.0.0.0–10.0.0.255. 10.0.1.50 has third octet = 1, outside the /24. "
            "Only 10.0.0.0/8 (10.x.x.x) contains the destination. LPM among matching routes "
            "is irrelevant when only one route matches."
        ),
    },
    {
        "id": "cd3v2-013",
        "domain": 3,
        "objective": "3.2",
        "type": "multiple_response",
        "difficulty": "expert",
        "study_topic": "Administrative distance",
        "stem": (
            "Which TWO statements about administrative distance are true? (Choose TWO.)"
        ),
        "options": [
            {
                "id": "a",
                "text": "AD is compared only between routes with the same prefix length to the same destination",
                "correct": True,
                "rationale": (
                    "Correct. AD resolves which protocol's route to install when multiple protocols "
                    "advertise the SAME prefix. If prefix lengths differ, longest-prefix match "
                    "already decided the winner and AD is not relevant."
                ),
            },
            {
                "id": "b",
                "text": "A route with AD 255 is considered unreachable and is never installed",
                "correct": True,
                "rationale": (
                    "Correct. AD 255 is reserved to mean 'this route is absolutely untrusted/unreachable' "
                    "and is never placed in the routing table. It effectively disables the route."
                ),
            },
            {
                "id": "c",
                "text": "A lower AD means a worse (less trustworthy) route source",
                "correct": False,
                "rationale": (
                    "Incorrect. LOWER AD = MORE trusted, MORE preferred. Connected (0) is most "
                    "trusted; RIP (120) is less trusted."
                ),
            },
            {
                "id": "d",
                "text": "AD is exchanged between routers as part of the routing update",
                "correct": False,
                "rationale": (
                    "Incorrect. AD is a local, router-specific value. It is never advertised "
                    "in routing updates; each router maintains its own AD table."
                ),
            },
        ],
        "explanation": (
            "AD is local and not exchanged; it determines which protocol's route installs for "
            "equal-length prefixes. Lower AD = more trusted. AD 255 = unreachable/unusable. "
            "AD is irrelevant when prefix lengths differ (LPM already decided)."
        ),
    },
    {
        "id": "cd3v2-014",
        "domain": 3,
        "objective": "3.2",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "Forwarding decision & longest prefix match",
        "stem": (
            "A router receives a packet destined for 192.0.2.1. Its routing table has:\n"
            "  192.0.2.0/24 via A  (AD 110)\n"
            "  192.0.2.0/24 via B  (AD 90)\n"
            "  192.0.2.0/24 via C  (AD 90, same metric as B)\n"
            "What happens?"
        ),
        "options": [
            {
                "id": "a",
                "text": "Routes via B and C are installed (equal-cost multipath); route via A is not installed",
                "correct": True,
                "rationale": (
                    "Correct. Same prefix, same length — AD is compared first. B and C both have "
                    "AD 90 (lower than A's 110), so A is excluded. B and C have equal AD AND equal "
                    "metric, so both are installed for ECMP (equal-cost multipath) load balancing."
                ),
            },
            {
                "id": "b",
                "text": "All three routes are installed and traffic is split three ways",
                "correct": False,
                "rationale": (
                    "Incorrect. A's AD of 110 is higher than B/C's 90, so A is not installed. "
                    "Only routes from the lowest-AD source (B and C) are installed."
                ),
            },
            {
                "id": "c",
                "text": "Only route via A is installed because OSPF (AD 110) is more stable",
                "correct": False,
                "rationale": (
                    "Incorrect. Lower AD wins: 90 (B and C) beats 110 (A). A is not installed."
                ),
            },
            {
                "id": "d",
                "text": "Only route via B is installed; C is ignored as a duplicate",
                "correct": False,
                "rationale": (
                    "Incorrect. Equal-cost paths from the same protocol are both installed for "
                    "load balancing (ECMP), not arbitrarily deduplicated."
                ),
            },
        ],
        "explanation": (
            "Selection order: (1) longest prefix (all /24 — tied), (2) lowest AD (B and C at 90 win "
            "over A at 110), (3) metric (B and C are equal → ECMP). Both B and C are installed; "
            "A is not. Maximum-paths limits how many ECMP routes are installed (default 4–16)."
        ),
    },
    # ------------------------------------------------ 3.3 static routing (7 Qs)
    {
        "id": "cd3v2-015",
        "domain": 3,
        "objective": "3.3",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "Static routing",
        "stem": (
            "An engineer configures:\n"
            "  ip route 10.0.0.0 255.0.0.0 Null0\n"
            "What is the purpose of routing to Null0?"
        ),
        "options": [
            {
                "id": "a",
                "text": (
                    "It creates a 'black-hole' route that silently discards matching traffic, "
                    "commonly used to prevent routing loops when summarizing"
                ),
                "correct": True,
                "rationale": (
                    "Correct. A Null0 static route drops all traffic matching the prefix without "
                    "sending an error (unless 'ip unreachables' is enabled). It is used as a "
                    "discard route, often paired with a summary advertisement to prevent "
                    "recursive routing loops for unallocated subnets."
                ),
            },
            {
                "id": "b",
                "text": "It sends matching traffic to the default gateway",
                "correct": False,
                "rationale": (
                    "Incorrect. Null0 is a virtual interface that discards packets; "
                    "traffic sent there is dropped, not forwarded to a gateway."
                ),
            },
            {
                "id": "c",
                "text": "It marks the route as a backup that activates only when the primary fails",
                "correct": False,
                "rationale": (
                    "Incorrect. That is a floating static route (using a high AD), "
                    "not a Null0 route. Null0 is always active as a discard."
                ),
            },
            {
                "id": "d",
                "text": "It is used for NAT overload, translating 10.0.0.0/8 addresses",
                "correct": False,
                "rationale": (
                    "Incorrect. NAT is configured with 'ip nat inside source' statements; "
                    "a Null0 route is purely a discard mechanism."
                ),
            },
        ],
        "explanation": (
            "A static route to Null0 creates a discard (black-hole) route. "
            "Routers summarizing a block (e.g., advertising 10.0.0.0/8) often install a "
            "Null0 route for that block so that traffic for unallocated subnets is dropped "
            "rather than looped via the default route."
        ),
    },
    {
        "id": "cd3v2-016",
        "domain": 3,
        "objective": "3.3",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "Static routing",
        "stem": (
            "An engineer types: 'ip route 192.168.1.0 255.255.255.0 10.0.0.1 name BRANCH1-PRIMARY'\n"
            "What does the 'name' keyword do?"
        ),
        "options": [
            {
                "id": "a",
                "text": "It adds a descriptive label to the static route for documentation; it has no effect on forwarding",
                "correct": True,
                "rationale": (
                    "Correct. The 'name' keyword in 'ip route' attaches a description to "
                    "the static route entry, visible in 'show ip route' and 'show running-config'. "
                    "It is purely administrative and does not influence forwarding behavior."
                ),
            },
            {
                "id": "b",
                "text": "It creates a named OSPF redistribution policy for this route",
                "correct": False,
                "rationale": (
                    "Incorrect. The 'name' keyword is solely a documentation tag on the static "
                    "route. OSPF redistribution is a separate configuration."
                ),
            },
            {
                "id": "c",
                "text": "It assigns this route to a VRF named BRANCH1-PRIMARY",
                "correct": False,
                "rationale": (
                    "Incorrect. VRF-specific static routes use 'ip route vrf <vrf-name>', "
                    "not the 'name' keyword."
                ),
            },
            {
                "id": "d",
                "text": "It tags the route so OSPF can redistribute it with tag BRANCH1-PRIMARY",
                "correct": False,
                "rationale": (
                    "Incorrect. Route tags for redistribution are set with 'tag <number>' in "
                    "route-maps. 'name' is a human-readable label with no routing function."
                ),
            },
        ],
        "explanation": (
            "The 'name' keyword in 'ip route' provides a human-readable description visible "
            "in the running configuration and routing table output. It has zero impact on "
            "forwarding decisions, AD, or metric."
        ),
    },
    {
        "id": "cd3v2-017",
        "domain": 3,
        "objective": "3.3",
        "type": "multiple_choice",
        "difficulty": "expert",
        "study_topic": "Floating static routes",
        "stem": (
            "R1 has a primary path to 10.5.5.0/24 learned via EIGRP (AD 90). "
            "A backup static route with AD 95 is also configured. "
            "The EIGRP neighbor goes down. Which 'show' command would confirm "
            "the floating static is now active?"
        ),
        "options": [
            {
                "id": "a",
                "text": "show ip route 10.5.5.0 — it will now show 'S 10.5.5.0/24 [95/0]'",
                "correct": True,
                "rationale": (
                    "Correct. Once the EIGRP route is withdrawn, the floating static (AD 95) "
                    "is the best available route. 'show ip route 10.5.5.0' will show it with "
                    "the code 'S' and [95/0], confirming it is now installed."
                ),
            },
            {
                "id": "b",
                "text": "show ip eigrp neighbors — it will show the backup route activating",
                "correct": False,
                "rationale": (
                    "Incorrect. 'show ip eigrp neighbors' shows EIGRP neighbor adjacencies, "
                    "not static route activation. The correct command to verify route installation "
                    "is 'show ip route'."
                ),
            },
            {
                "id": "c",
                "text": "show ip route 10.5.5.0 will show both the EIGRP and static routes simultaneously",
                "correct": False,
                "rationale": (
                    "Incorrect. If the EIGRP neighbor is down, the EIGRP route is removed. "
                    "Only the floating static would appear after EIGRP fails."
                ),
            },
            {
                "id": "d",
                "text": "debug ip routing — it is the only way to confirm a floating static is active",
                "correct": False,
                "rationale": (
                    "Incorrect. 'debug ip routing' helps observe routing table changes in real time, "
                    "but 'show ip route' is the straightforward operational verification command."
                ),
            },
        ],
        "explanation": (
            "When the EIGRP route (AD 90) is withdrawn, the floating static (AD 95) becomes the "
            "best available route and is installed. 'show ip route' confirms this with code 'S' "
            "and [95/0]. Floating statics are invisible in the table while the primary is present."
        ),
    },
    {
        "id": "cd3v2-018",
        "domain": 3,
        "objective": "3.3",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "Floating static routes",
        "stem": (
            "Two ISPs provide WAN connectivity. ISP-A is the preferred path (OSPF, AD 110). "
            "ISP-B is a backup connected via a static route. The static must NOT be used "
            "unless ISP-A is completely down. The engineer configures:\n"
            "  ip route 0.0.0.0 0.0.0.0 203.0.113.2 115\n"
            "Will this work correctly as a floating static?"
        ),
        "options": [
            {
                "id": "a",
                "text": (
                    "No — AD 115 is still LOWER than OSPF's AD of 110 for IS-IS, but competes "
                    "with OSPF (AD 110): 115 > 110, so the static DOES float behind OSPF. "
                    "Actually it IS correct — the static (115) loses to OSPF (110) and floats."
                ),
                "correct": True,
                "rationale": (
                    "Correct. OSPF's default AD is 110. The static route's AD of 115 is HIGHER "
                    "than 110, so OSPF's default route is preferred and the static floats — "
                    "it is only installed when the OSPF route is gone. The configuration achieves "
                    "the backup-only goal. (IS-IS is AD 115, but the question is about OSPF vs static.)"
                ),
            },
            {
                "id": "b",
                "text": "No — the static (AD 115) beats OSPF (AD 110) because 115 > 110",
                "correct": False,
                "rationale": (
                    "Incorrect. LOWER AD wins, not higher. 110 < 115, so OSPF (110) is preferred "
                    "over the static (115). The static correctly floats behind OSPF."
                ),
            },
            {
                "id": "c",
                "text": "No — floating static routes only work with a next-hop IP, not with 0.0.0.0/0",
                "correct": False,
                "rationale": (
                    "Incorrect. Floating static routes can use any prefix including 0.0.0.0/0; "
                    "the technique works for default routes as well as specific prefixes."
                ),
            },
            {
                "id": "d",
                "text": "No — you must set the static AD to exactly 111 to float behind OSPF",
                "correct": False,
                "rationale": (
                    "Incorrect. Any AD higher than the primary's works; 111, 115, 120, or any "
                    "value above 110 would cause the static to float. 115 is a valid choice."
                ),
            },
        ],
        "explanation": (
            "For a floating static to back up OSPF (AD 110), set the static's AD above 110. "
            "AD 115 > 110 means the static loses to OSPF and is NOT installed while OSPF "
            "provides the route. When OSPF's route disappears, the static (AD 115) installs. "
            "Remember: lower AD = more preferred."
        ),
    },
    {
        "id": "cd3v2-019",
        "domain": 3,
        "objective": "3.3",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "IPv6 static routing",
        "stem": (
            "An engineer types: 'ipv6 route 2001:db8:1::/48 GigabitEthernet0/0'\n"
            "The interface G0/0 connects to a point-to-point serial emulation over Ethernet "
            "to a single neighbor at 2001:db8:0:12::2. Weeks later, pings to 2001:db8:1::1 fail. "
            "What is the MOST likely cause related to this static route configuration?"
        ),
        "options": [
            {
                "id": "a",
                "text": (
                    "Using only an exit interface on a multi-access Ethernet link forces the router "
                    "to perform neighbor discovery for every destination, which can fail if the "
                    "neighbor's link-local is not in the neighbor cache"
                ),
                "correct": True,
                "rationale": (
                    "Correct. On a multi-access (Ethernet) link with exit-interface-only, the "
                    "router must resolve the destination's IPv6 link-local via Neighbor Discovery. "
                    "If the neighbor cache expires or the neighbor isn't reachable, forwarding fails "
                    "even if the static route is installed. Best practice: specify the link-local "
                    "next-hop or use both interface and next-hop."
                ),
            },
            {
                "id": "b",
                "text": "The command is invalid because IPv6 static routes cannot use exit interfaces",
                "correct": False,
                "rationale": (
                    "Incorrect. IPv6 static routes can use exit interfaces, next-hop addresses, "
                    "or both. The command is syntactically valid."
                ),
            },
            {
                "id": "c",
                "text": "The prefix 2001:db8:1::/48 is too large for a static route",
                "correct": False,
                "rationale": (
                    "Incorrect. There is no size restriction on static route prefixes; "
                    "/48 is a completely valid IPv6 prefix length."
                ),
            },
            {
                "id": "d",
                "text": "The 'ipv6 unicast-routing' command was not issued, so IPv6 static routes are ignored",
                "correct": False,
                "rationale": (
                    "Incorrect. While 'ipv6 unicast-routing' IS required for routing, if it were "
                    "missing, ALL IPv6 routing would fail, not just this specific scenario. "
                    "The question hints at a more specific forwarding issue related to the exit-interface-only config."
                ),
            },
        ],
        "explanation": (
            "IPv6 static routes with exit-interface-only on Ethernet work like IPv4: the router "
            "must resolve the next-hop via NDP (Neighbor Discovery). If the ND cache entry times out "
            "and NDP fails, forwarding breaks. Specifying both the exit interface AND the link-local "
            "next-hop (e.g., 'ipv6 route ... G0/0 FE80::...') is more reliable."
        ),
    },
    {
        "id": "cd3v2-020",
        "domain": 3,
        "objective": "3.3",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "IPv6 static routing",
        "stem": (
            "Which command adds an IPv6 static route for 2001:db8:cafe::/48 using "
            "link-local next-hop FE80::1 on interface GigabitEthernet0/0?"
        ),
        "options": [
            {
                "id": "a",
                "text": "ipv6 route 2001:db8:cafe::/48 GigabitEthernet0/0 FE80::1",
                "correct": True,
                "rationale": (
                    "Correct. When using a link-local address as the IPv6 next-hop, the exit "
                    "interface MUST also be specified because link-local addresses are not unique "
                    "globally. The syntax is 'ipv6 route <prefix> <exit-intf> <link-local-nexthop>'."
                ),
            },
            {
                "id": "b",
                "text": "ipv6 route 2001:db8:cafe::/48 FE80::1",
                "correct": False,
                "rationale": (
                    "Incorrect. A link-local address (FE80::) as next-hop requires an exit "
                    "interface to disambiguate which link the address belongs to. "
                    "Omitting the interface causes an error or ambiguity."
                ),
            },
            {
                "id": "c",
                "text": "ip route 2001:db8:cafe::/48 GigabitEthernet0/0 FE80::1",
                "correct": False,
                "rationale": (
                    "Incorrect. 'ip route' is for IPv4 only; IPv6 static routes use 'ipv6 route'."
                ),
            },
            {
                "id": "d",
                "text": "ipv6 route 2001:db8:cafe::/48 FE80::1 GigabitEthernet0/0",
                "correct": False,
                "rationale": (
                    "Incorrect. In IOS syntax the exit interface comes BEFORE the next-hop "
                    "when both are specified: 'ipv6 route <prefix> <intf> <nexthop>'. "
                    "Reversing the order is not valid."
                ),
            },
        ],
        "explanation": (
            "IPv6 static route with a link-local next-hop: 'ipv6 route <prefix> <exit-interface> <FE80::next-hop>'. "
            "The exit interface is mandatory with link-local next-hops because FE80:: addresses "
            "are only unique per-link. Global unicast next-hops do not require the interface."
        ),
    },
    {
        "id": "cd3v2-021",
        "domain": 3,
        "objective": "3.3",
        "type": "multiple_response",
        "difficulty": "expert",
        "study_topic": "Static routing",
        "stem": (
            "Which TWO conditions cause a static route to be REMOVED from the routing table "
            "automatically? (Choose TWO.)"
        ),
        "options": [
            {
                "id": "a",
                "text": "The exit interface specified in the static route goes down",
                "correct": True,
                "rationale": (
                    "Correct. If the route is configured with an exit interface (or a fully specified "
                    "route where the interface goes down), IOS removes it from the table because "
                    "the interface is no longer available."
                ),
            },
            {
                "id": "b",
                "text": "The next-hop IP address becomes unreachable (no route to the next-hop)",
                "correct": True,
                "rationale": (
                    "Correct. For a next-hop-only static route, if the next-hop address itself "
                    "becomes unreachable (recursive lookup fails), the route is removed from "
                    "the active table (marked 'possibly down' or removed entirely)."
                ),
            },
            {
                "id": "c",
                "text": "A routing protocol advertises a route to the same destination",
                "correct": False,
                "rationale": (
                    "Incorrect. A routing protocol advertising the same prefix does not remove "
                    "a static route from configuration. The static route stays configured; "
                    "if its AD is lower, it still wins. The routing protocol just adds its own entry."
                ),
            },
            {
                "id": "d",
                "text": "The router is rebooted without saving the configuration",
                "correct": False,
                "rationale": (
                    "Incorrect. This removes the route from the running configuration "
                    "(unsaved config is lost), not an 'automatic' removal from the routing table "
                    "while the router is operational. The question asks about automatic removal "
                    "during operation."
                ),
            },
        ],
        "explanation": (
            "Static routes are automatically removed from the active routing table when: "
            "(1) the exit interface goes down, or (2) the next-hop is unreachable (recursive lookup fails). "
            "They remain in the running config but are not used for forwarding until the condition resolves."
        ),
    },
    # ------------------------------------------------------- 3.4 OSPF (13 Qs)
    {
        "id": "cd3v2-022",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_choice",
        "difficulty": "expert",
        "study_topic": "OSPF DR/BDR election",
        "stem": (
            "Five routers share a broadcast Ethernet segment. Their OSPF priorities and RIDs are:\n"
            "  R1: priority 5, RID 5.5.5.5\n"
            "  R2: priority 5, RID 2.2.2.2\n"
            "  R3: priority 0, RID 9.9.9.9\n"
            "  R4: priority 1, RID 4.4.4.4\n"
            "  R5: priority 5, RID 5.5.5.4\n"
            "Which router becomes the DR and which becomes the BDR?"
        ),
        "options": [
            {
                "id": "a",
                "text": "DR = R1 (priority 5, highest RID among tied-priority routers); BDR = R5",
                "correct": True,
                "rationale": (
                    "Correct. Step 1: highest priority — R1, R2, R5 all have priority 5; "
                    "R3 is disqualified (priority 0); R4 has priority 1. "
                    "Step 2: tiebreak by highest RID among priority-5 routers: "
                    "R1 (5.5.5.5) > R5 (5.5.5.4) > R2 (2.2.2.2). "
                    "DR = R1. For BDR, next highest RID among remaining priority-5: R5 (5.5.5.4). BDR = R5."
                ),
            },
            {
                "id": "b",
                "text": "DR = R3 because it has the highest RID (9.9.9.9)",
                "correct": False,
                "rationale": (
                    "Incorrect. R3 has priority 0, which permanently disqualifies it from "
                    "DR and BDR election. RID tiebreak only applies among routers with equal "
                    "POSITIVE priority."
                ),
            },
            {
                "id": "c",
                "text": "DR = R4 (priority 1) because it is the only router with a unique priority",
                "correct": False,
                "rationale": (
                    "Incorrect. Higher priority wins, not uniqueness. R4's priority 1 is lower "
                    "than R1/R2/R5's priority 5, so R4 does not become DR."
                ),
            },
            {
                "id": "d",
                "text": "DR = R2 (priority 5, highest RID among priority-5 group is 2.2.2.2)",
                "correct": False,
                "rationale": (
                    "Incorrect. Among priority-5 routers (R1, R2, R5), the highest RID is "
                    "R1's 5.5.5.5, not R2's 2.2.2.2. DR is R1."
                ),
            },
        ],
        "explanation": (
            "OSPF DR/BDR on broadcast: (1) highest interface priority wins; priority 0 = ineligible. "
            "(2) Tiebreak among equal-priority eligible routers: highest RID. "
            "Priority 5 group: 5.5.5.5 > 5.5.5.4 > 2.2.2.2 → DR=R1, BDR=R5. "
            "R4 (priority 1) becomes DROTHER."
        ),
    },
    {
        "id": "cd3v2-023",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_choice",
        "difficulty": "expert",
        "study_topic": "OSPF cost & configuration",
        "stem": (
            "An OSPF topology has a path from R1 to R5 via two possible routes:\n"
            "  Path A: R1(G0/0, cost 4) → R2(G0/1, cost 10) → R5  (total accumulated cost at R5)\n"
            "  Path B: R1(G0/1, cost 1) → R3(G0/0, cost 1) → R4(G0/0, cost 1) → R5  (3 hops)\n"
            "OSPF always sums the cost of OUTGOING interfaces. Assuming all links are the same "
            "direction, what are the total costs for Path A and Path B, and which does OSPF prefer?"
        ),
        "options": [
            {
                "id": "a",
                "text": "Path A = 14, Path B = 3; OSPF prefers Path B (lower cost)",
                "correct": True,
                "rationale": (
                    "Correct. Path A: outgoing costs are G0/0 (4) + G0/1 (10) = 14. "
                    "Path B: 1 + 1 + 1 = 3. OSPF's SPF algorithm selects the lowest-cost path; "
                    "Path B (cost 3) beats Path A (cost 14), regardless of hop count."
                ),
            },
            {
                "id": "b",
                "text": "Path A = 14, Path B = 3; OSPF prefers Path A because it has fewer hops",
                "correct": False,
                "rationale": (
                    "Incorrect. OSPF uses cost, NOT hop count. Fewer hops is RIP's metric; "
                    "OSPF selects minimum cumulative cost, which is Path B (3)."
                ),
            },
            {
                "id": "c",
                "text": "Path A = 10, Path B = 1; OSPF uses only the highest single-link cost",
                "correct": False,
                "rationale": (
                    "Incorrect. OSPF cost is additive (sum of all outgoing interface costs), "
                    "not the maximum single link."
                ),
            },
            {
                "id": "d",
                "text": "OSPF load-balances across both paths because they are from the same protocol",
                "correct": False,
                "rationale": (
                    "Incorrect. OSPF load-balances only when costs are EQUAL. Path A (14) ≠ Path B (3), "
                    "so OSPF selects only Path B."
                ),
            },
        ],
        "explanation": (
            "OSPF metric = sum of outgoing interface costs. Path A: 4+10=14. Path B: 1+1+1=3. "
            "SPF installs Path B. OSPF equal-cost load balancing requires identical cumulative costs. "
            "Hop count is irrelevant to OSPF."
        ),
    },
    {
        "id": "cd3v2-024",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "OSPF cost & configuration",
        "stem": (
            "A network has 10G links throughout. With the default OSPF reference bandwidth (100 Mbps), "
            "all 10G interfaces show cost 1. An engineer wants 1G links to have cost 10 and "
            "10G links to have cost 1. Which command achieves this?"
        ),
        "options": [
            {
                "id": "a",
                "text": "auto-cost reference-bandwidth 10000 (configured under 'router ospf')",
                "correct": True,
                "rationale": (
                    "Correct. 'auto-cost reference-bandwidth 10000' sets the reference to 10,000 Mbps "
                    "(10 Gbps). Cost formula: 10000/1000 (1G) = 10; 10000/10000 (10G) = 1. "
                    "This correctly differentiates 1G and 10G links."
                ),
            },
            {
                "id": "b",
                "text": "auto-cost reference-bandwidth 1000",
                "correct": False,
                "rationale": (
                    "Incorrect. 1000 Mbps reference: 1000/1000 (1G) = 1; 1000/10000 (10G) = 0.1 → 1. "
                    "Both 1G and 10G would still have cost 1, not differentiating them."
                ),
            },
            {
                "id": "c",
                "text": "ip ospf cost 10 on each 1G interface individually",
                "correct": False,
                "rationale": (
                    "Incorrect. While per-interface 'ip ospf cost' works, the question asks "
                    "for the global reference-bandwidth approach that automatically differentiates "
                    "all interfaces by speed. 'auto-cost reference-bandwidth 10000' is the "
                    "correct systemic solution."
                ),
            },
            {
                "id": "d",
                "text": "auto-cost reference-bandwidth 100000",
                "correct": False,
                "rationale": (
                    "Incorrect. 100,000 Mbps reference: 1G cost = 100, 10G cost = 10. "
                    "While this differentiates them, the engineer specifically wants 1G=10 and "
                    "10G=1, which requires reference 10000, not 100000."
                ),
            },
        ],
        "explanation": (
            "OSPF cost = reference-bandwidth (Mbps) / interface-bandwidth (Mbps), minimum 1. "
            "To get 1G=10 and 10G=1: set reference to 10,000 Mbps (10 Gbps). "
            "10000/1000=10, 10000/10000=1. Must configure on ALL routers consistently."
        ),
    },
    {
        "id": "cd3v2-025",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "OSPF adjacencies",
        "stem": (
            "Two OSPF routers (R1 and R2) are connected via Ethernet. R1 is in area 0, "
            "R2 is in area 1 on the same interface. Will they form a neighbor relationship?"
        ),
        "options": [
            {
                "id": "a",
                "text": "No — OSPF interfaces must be in the same area to form an adjacency; area mismatch prevents it",
                "correct": True,
                "rationale": (
                    "Correct. OSPF routers can only form adjacencies through interfaces configured "
                    "in the SAME area. If R1's interface is in area 0 and R2's is in area 1, "
                    "the area ID in their hello packets differs and they will not proceed past "
                    "the INIT state."
                ),
            },
            {
                "id": "b",
                "text": "Yes — they become neighbors and the link becomes an inter-area link automatically",
                "correct": False,
                "rationale": (
                    "Incorrect. OSPF does not auto-negotiate inter-area links. Both ends of a "
                    "link must be in the same area for adjacency. An ABR has interfaces in "
                    "different areas but each interface belongs to exactly one."
                ),
            },
            {
                "id": "c",
                "text": "Yes — area ID only matters for LSA flooding, not for adjacency formation",
                "correct": False,
                "rationale": (
                    "Incorrect. Area ID is one of the parameters checked during hello exchange. "
                    "A mismatch prevents adjacency formation, not just LSA flooding."
                ),
            },
            {
                "id": "d",
                "text": "It depends on the router ID; if RIDs are in different ranges the areas don't matter",
                "correct": False,
                "rationale": (
                    "Incorrect. Router IDs are just identifiers (32-bit values) with no relation "
                    "to area assignment. Area IDs must match for adjacency."
                ),
            },
        ],
        "explanation": (
            "OSPF hello parameters that MUST match for adjacency: area ID, subnet/mask, "
            "hello/dead timers, authentication, and MTU. Area ID mismatch is detected "
            "immediately in hello exchange and prevents the neighbor from advancing past INIT."
        ),
    },
    {
        "id": "cd3v2-026",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_choice",
        "difficulty": "expert",
        "study_topic": "OSPF adjacencies",
        "stem": (
            "On a broadcast segment with DR=R1, BDR=R2, DROTHERs=R3 and R4. "
            "What neighbor state will R3 maintain with R4 under normal OSPF operation?"
        ),
        "options": [
            {
                "id": "a",
                "text": "2-WAY — DROTHERs exchange hellos with each other but do not fully synchronize databases",
                "correct": True,
                "rationale": (
                    "Correct. On broadcast segments, DROTHER routers (not DR/BDR) form 2-WAY "
                    "adjacency with each other (they see each other in hello packets) but do NOT "
                    "proceed to FULL. Full adjacency (database synchronization) only occurs between "
                    "each DROTHER and the DR/BDR."
                ),
            },
            {
                "id": "b",
                "text": "FULL — all OSPF routers on a segment reach FULL state with each other",
                "correct": False,
                "rationale": (
                    "Incorrect. On broadcast networks, DROTHERs only go FULL with the DR and BDR. "
                    "DROTHER-to-DROTHER relationships stay at 2-WAY, reducing flooding overhead."
                ),
            },
            {
                "id": "c",
                "text": "INIT — DROTHERs never see each other's hellos",
                "correct": False,
                "rationale": (
                    "Incorrect. DROTHERs DO see each other's hellos (and each other's RIDs in "
                    "the hello), so they reach 2-WAY. INIT means only one-way hello reception."
                ),
            },
            {
                "id": "d",
                "text": "EXSTART — DROTHERs continuously attempt database exchange with each other",
                "correct": False,
                "rationale": (
                    "Incorrect. DROTHERs deliberately stop at 2-WAY with each other. "
                    "EXSTART and beyond occur only between DROTHERs and the DR/BDR."
                ),
            },
        ],
        "explanation": (
            "On broadcast multi-access networks: DROTHERs → DR/BDR: FULL adjacency (database sync). "
            "DROTHERs → DROTHERs: 2-WAY only (see each other's hellos, no DB exchange). "
            "This is intentional to reduce n(n-1)/2 DB exchanges to a star topology via the DR."
        ),
    },
    {
        "id": "cd3v2-027",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "OSPF cost & configuration",
        "stem": (
            "An engineer needs to influence OSPF path selection so that a specific interface "
            "is less preferred. Which interface-level command sets the OSPF cost to 500 on G0/2?"
        ),
        "options": [
            {
                "id": "a",
                "text": "ip ospf cost 500 (interface subcommand under G0/2)",
                "correct": True,
                "rationale": (
                    "Correct. 'ip ospf cost <value>' on the interface directly sets the OSPF cost, "
                    "overriding the auto-calculated value. Setting it to 500 makes the path "
                    "through G0/2 less attractive (higher cost = less preferred by SPF)."
                ),
            },
            {
                "id": "b",
                "text": "ospf cost 500 (global configuration)",
                "correct": False,
                "rationale": (
                    "Incorrect. 'ospf cost' is not a global command; it is an interface subcommand "
                    "('ip ospf cost') applied under the interface configuration mode."
                ),
            },
            {
                "id": "c",
                "text": "auto-cost reference-bandwidth 500 (under router ospf)",
                "correct": False,
                "rationale": (
                    "Incorrect. Changing the reference bandwidth affects ALL interfaces globally, "
                    "not a single interface, and produces calculated costs — not a direct assignment."
                ),
            },
            {
                "id": "d",
                "text": "ip metric 500 (interface subcommand)",
                "correct": False,
                "rationale": (
                    "Incorrect. 'ip metric 500' is not a valid IOS interface command for OSPF; "
                    "the correct command is 'ip ospf cost <value>'."
                ),
            },
        ],
        "explanation": (
            "'ip ospf cost <1-65535>' on a specific interface overrides the auto-calculated OSPF cost. "
            "Higher cost makes the path less preferred during SPF. This is the precise, "
            "single-interface method versus globally adjusting the reference bandwidth."
        ),
    },
    {
        "id": "cd3v2-028",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_choice",
        "difficulty": "expert",
        "study_topic": "OSPF DR/BDR election",
        "stem": (
            "The current DR on a segment fails. R2 was the BDR. What is the OSPF recovery sequence?"
        ),
        "options": [
            {
                "id": "a",
                "text": (
                    "R2 (former BDR) immediately promotes to DR; a new BDR election then occurs "
                    "among remaining DROTHER routers"
                ),
                "correct": True,
                "rationale": (
                    "Correct. When the DR fails, the BDR is pre-selected and immediately assumes "
                    "the DR role without a new DR election (this is the BDR's purpose — fast failover). "
                    "A new BDR is then elected from the remaining DROTHERs based on priority/RID."
                ),
            },
            {
                "id": "b",
                "text": "A full new election occurs among all remaining routers to pick a new DR",
                "correct": False,
                "rationale": (
                    "Incorrect. OSPF avoids a full re-election by pre-electing the BDR. "
                    "The BDR promotes to DR immediately, minimizing convergence delay."
                ),
            },
            {
                "id": "c",
                "text": "All adjacencies are torn down and rebuilt from scratch",
                "correct": False,
                "rationale": (
                    "Incorrect. Only the adjacency with the failed DR is lost; the BDR's "
                    "promotion to DR preserves existing adjacencies with the other DROTHERs."
                ),
            },
            {
                "id": "d",
                "text": "The router with the highest priority among DROTHERs immediately becomes the new DR",
                "correct": False,
                "rationale": (
                    "Incorrect. The BDR (not an arbitrary DROTHER) promotes to DR. "
                    "This is precisely why the BDR exists — for fast, orderly failover."
                ),
            },
        ],
        "explanation": (
            "OSPF DR failover: BDR promotes to DR immediately. A new BDR election then selects "
            "the best DROTHER (highest priority, then highest RID). This two-level approach means "
            "only one election is needed on DR failure, with minimal disruption."
        ),
    },
    {
        "id": "cd3v2-029",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "OSPF adjacencies",
        "stem": (
            "An OSPF adjacency between R1 and R2 is stuck in EXCHANGE state. "
            "Which of the following is a likely cause?"
        ),
        "options": [
            {
                "id": "a",
                "text": "An MTU mismatch — R1 sends DBD packets larger than R2's MTU, causing R2 to discard them",
                "correct": True,
                "rationale": (
                    "Correct. During EXCHANGE, routers send Database Description (DBD) packets. "
                    "If R1's MTU is larger than R2's, R2 discards oversized DBDs and the exchange "
                    "never completes. The adjacency stalls in EXCHANGE (sometimes visible as "
                    "EXSTART/EXCHANGE cycling)."
                ),
            },
            {
                "id": "b",
                "text": "Mismatched OSPF process IDs",
                "correct": False,
                "rationale": (
                    "Incorrect. Process IDs are locally significant; a mismatch does not prevent "
                    "adjacency — the routers don't even exchange process IDs."
                ),
            },
            {
                "id": "c",
                "text": "One router has no loopback interface configured",
                "correct": False,
                "rationale": (
                    "Incorrect. A loopback is not required for OSPF adjacency; its only "
                    "role is in router-ID selection, which is resolved before EXCHANGE state."
                ),
            },
            {
                "id": "d",
                "text": "The routers are in different autonomous systems",
                "correct": False,
                "rationale": (
                    "Incorrect. OSPF uses areas, not autonomous systems (that is BGP/EIGRP terminology). "
                    "OSPF routers in different areas CAN form adjacencies on the same link "
                    "(they just need the same area ID on that interface)."
                ),
            },
        ],
        "explanation": (
            "OSPF EXCHANGE state stall: most commonly MTU mismatch. R1 sends DBDs larger than "
            "R2's interface MTU; R2 silently drops them. Workaround: 'ip ospf mtu-ignore' or "
            "matching MTUs. Other causes include duplicate RIDs or authentication mismatch."
        ),
    },
    {
        "id": "cd3v2-030",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_response",
        "difficulty": "expert",
        "study_topic": "OSPF cost & configuration",
        "stem": (
            "Which TWO methods can be used to add an interface to an OSPF process and assign it to an area "
            "in Cisco IOS? (Choose TWO.)"
        ),
        "options": [
            {
                "id": "a",
                "text": "Under 'router ospf <pid>': network <address> <wildcard> area <area-id>",
                "correct": True,
                "rationale": (
                    "Correct. The traditional method uses 'network' statements with wildcard masks "
                    "under the OSPF process to match interface addresses and assign them to areas."
                ),
            },
            {
                "id": "b",
                "text": "Under the interface: ip ospf <pid> area <area-id>",
                "correct": True,
                "rationale": (
                    "Correct. The per-interface method 'ip ospf <process-id> area <area-id>' "
                    "directly enables OSPF on the interface and assigns its area, without "
                    "needing a 'network' statement in the OSPF process."
                ),
            },
            {
                "id": "c",
                "text": "Under the interface: ospf enable area <area-id>",
                "correct": False,
                "rationale": (
                    "Incorrect. 'ospf enable area' is not valid IOS syntax. "
                    "The correct interface command is 'ip ospf <pid> area <area-id>'."
                ),
            },
            {
                "id": "d",
                "text": "Under 'router ospf <pid>': area <area-id> interface <intf-name>",
                "correct": False,
                "rationale": (
                    "Incorrect. 'area <n> interface' is not a valid OSPF process subcommand for "
                    "enabling OSPF on interfaces; interfaces are matched via 'network' statements "
                    "or the per-interface 'ip ospf' command."
                ),
            },
        ],
        "explanation": (
            "Two valid methods to add an interface to OSPF: "
            "(1) 'network <addr> <wildcard> area <n>' under the OSPF process, or "
            "(2) 'ip ospf <pid> area <n>' directly on the interface. "
            "Both are supported and can coexist; the per-interface method is more precise."
        ),
    },
    {
        "id": "cd3v2-031",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "OSPF adjacencies",
        "stem": (
            "R1 and R2 are OSPF neighbors. R1 has hello interval 10s / dead interval 40s. "
            "An engineer changes R2's hello interval to 5s but leaves its dead interval at 40s. "
            "What happens to the adjacency?"
        ),
        "options": [
            {
                "id": "a",
                "text": "The adjacency drops — OSPF requires hello intervals to match between neighbors",
                "correct": True,
                "rationale": (
                    "Correct. Hello and dead intervals must match between OSPF neighbors. "
                    "R1 (hello 10s) and R2 (hello 5s) will see mismatched values in each other's "
                    "hello packets and will not maintain the adjacency."
                ),
            },
            {
                "id": "b",
                "text": "The adjacency is unaffected; only the dead interval must match",
                "correct": False,
                "rationale": (
                    "Incorrect. Both hello AND dead intervals must match. In this scenario the "
                    "dead intervals are equal (40s) but the hello intervals differ, which still "
                    "breaks the adjacency."
                ),
            },
            {
                "id": "c",
                "text": "R1 automatically adjusts its hello interval to match R2's 5s",
                "correct": False,
                "rationale": (
                    "Incorrect. OSPF does not auto-negotiate timer values from neighbor hellos; "
                    "they must be manually configured to match."
                ),
            },
            {
                "id": "d",
                "text": "The adjacency stays up but LSAs are flooded at R2's 5s rate",
                "correct": False,
                "rationale": (
                    "Incorrect. The adjacency drops due to hello interval mismatch. "
                    "LSA flooding rates are unrelated to the hello/dead mismatch issue."
                ),
            },
        ],
        "explanation": (
            "OSPF hello and dead intervals are checked in received hello packets. "
            "If either value doesn't match, the neighbor relationship fails. "
            "R1 sees R2's hello (hello=5s) and rejects it because R1 expects 10s. "
            "Must use 'ip ospf hello-interval' and 'ip ospf dead-interval' to synchronize."
        ),
    },
    {
        "id": "cd3v2-032",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_choice",
        "difficulty": "expert",
        "study_topic": "OSPF DR/BDR election",
        "stem": (
            "An engineer wants to ensure Router R1 is always the DR on a segment and never gets "
            "displaced by any future routers added to the segment. Which TWO steps should be taken? "
            "(This is a multiple-choice question with one correct compound answer.)"
        ),
        "options": [
            {
                "id": "a",
                "text": (
                    "Set R1's OSPF priority to 255 (maximum) and configure 'ip ospf priority 0' "
                    "on all other routers' interfaces to make them ineligible"
                ),
                "correct": True,
                "rationale": (
                    "Correct. Setting R1 to priority 255 (max) ensures it wins any fresh election. "
                    "Setting other routers to priority 0 prevents them from ever becoming DR or BDR, "
                    "guaranteeing R1 is always DR (for new elections). Note: existing DR is "
                    "non-preemptive, so a 'clear ip ospf process' may be needed to trigger a "
                    "fresh election after configuration."
                ),
            },
            {
                "id": "b",
                "text": "Enable OSPF preemption so R1 takes over immediately when it has the highest priority",
                "correct": False,
                "rationale": (
                    "Incorrect. OSPF does NOT support preemption for DR/BDR — this is a key "
                    "difference from HSRP. Once a DR is elected it stays until it fails or "
                    "OSPF is reset. There is no 'preempt' option for OSPF DR."
                ),
            },
            {
                "id": "c",
                "text": "Set R1's router ID to 255.255.255.255 so it always wins the RID tiebreak",
                "correct": False,
                "rationale": (
                    "Incorrect. A high RID only helps when priorities are equal. The reliable method "
                    "is to set R1's priority to 255 and others to 0, not to rely on RID."
                ),
            },
            {
                "id": "d",
                "text": "Configure 'ip ospf dr-always' on R1's interface",
                "correct": False,
                "rationale": (
                    "Incorrect. 'ip ospf dr-always' is not a valid IOS command. "
                    "Priority manipulation is the correct approach."
                ),
            },
        ],
        "explanation": (
            "To lock in a DR: set the desired router's OSPF interface priority to 255 (max) and "
            "set all others to 0 (ineligible). OSPF DR election is non-preemptive, so a "
            "'clear ip ospf process' on all routers is needed to trigger a new election after changes. "
            "There is no OSPF 'preempt' like HSRP has."
        ),
    },
    {
        "id": "cd3v2-033",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "OSPF adjacencies",
        "stem": (
            "What is the purpose of the OSPF LOADING state?"
        ),
        "options": [
            {
                "id": "a",
                "text": "The router sends Link State Requests (LSRs) to get full details of LSAs it needs after the database description exchange",
                "correct": True,
                "rationale": (
                    "Correct. After EXCHANGE (DBD summary exchange), if a router identifies LSAs "
                    "it doesn't have, it enters LOADING and sends LSR packets to request full copies. "
                    "The neighbor responds with Link State Updates (LSUs). Once all LSAs are received, "
                    "the state advances to FULL."
                ),
            },
            {
                "id": "b",
                "text": "The router is loading its configuration from NVRAM",
                "correct": False,
                "rationale": (
                    "Incorrect. OSPF neighbor states are protocol states, unrelated to configuration "
                    "loading from NVRAM. LOADING specifically means exchanging LSAs, not system boot."
                ),
            },
            {
                "id": "c",
                "text": "The router is recalculating the SPF algorithm after a topology change",
                "correct": False,
                "rationale": (
                    "Incorrect. SPF recalculation occurs after FULL adjacency is reached and "
                    "a topology change LSA arrives. LOADING is about initial database synchronization."
                ),
            },
            {
                "id": "d",
                "text": "The routers are negotiating master/slave roles and sequence numbers",
                "correct": False,
                "rationale": (
                    "Incorrect. Master/slave negotiation happens in the EXSTART state, not LOADING. "
                    "LOADING occurs after EXCHANGE once DBD summary exchange is complete."
                ),
            },
        ],
        "explanation": (
            "OSPF states: Down → Init → 2-Way → ExStart (master/slave) → Exchange (DBDs) → "
            "Loading (LSR/LSU for missing LSAs) → Full (synchronized). "
            "LOADING is the final synchronization step before FULL adjacency."
        ),
    },
    {
        "id": "cd3v2-034",
        "domain": 3,
        "objective": "3.4",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "OSPF cost & configuration",
        "stem": (
            "A router has 'passive-interface default' configured under OSPF, then 'no passive-interface GigabitEthernet0/0'. "
            "What is the net effect?"
        ),
        "options": [
            {
                "id": "a",
                "text": (
                    "All interfaces are passive (no OSPF hellos) EXCEPT GigabitEthernet0/0, "
                    "which actively sends hellos and can form adjacencies"
                ),
                "correct": True,
                "rationale": (
                    "Correct. 'passive-interface default' makes ALL interfaces passive. "
                    "'no passive-interface G0/0' then carves out G0/0 as the only active "
                    "OSPF interface. This is efficient when most interfaces face end hosts "
                    "and only a few need to peer."
                ),
            },
            {
                "id": "b",
                "text": "Only GigabitEthernet0/0 is passive; all other interfaces are active",
                "correct": False,
                "rationale": (
                    "Incorrect. 'passive-interface default' passivates ALL interfaces first; "
                    "'no passive-interface G0/0' then REMOVES G0/0 from the passive list, "
                    "making G0/0 active — the opposite of what this answer states."
                ),
            },
            {
                "id": "c",
                "text": "The commands cancel each other out and all interfaces behave as default active",
                "correct": False,
                "rationale": (
                    "Incorrect. 'passive-interface default' still applies to all interfaces "
                    "except G0/0, which was explicitly un-passived. The net effect is selective."
                ),
            },
            {
                "id": "d",
                "text": "OSPF stops running entirely because of the conflicting commands",
                "correct": False,
                "rationale": (
                    "Incorrect. These commands are complementary, not conflicting; IOS applies "
                    "them in order and the result is well-defined."
                ),
            },
        ],
        "explanation": (
            "'passive-interface default' under router ospf passivates all interfaces. "
            "'no passive-interface <intf>' selectively activates specific interfaces. "
            "This pattern is recommended in security hardening guides: default passive, "
            "then allow only the peering interfaces."
        ),
    },
    # ----------------------------------------- 3.5 FHRP (6 Qs)
    {
        "id": "cd3v2-035",
        "domain": 3,
        "objective": "3.5",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "First hop redundancy (HSRP/VRRP/GLBP)",
        "stem": (
            "In HSRP, the active router sends periodic hello messages to the standby. "
            "If the standby stops receiving hellos for the hold-time duration, what happens?"
        ),
        "options": [
            {
                "id": "a",
                "text": "The standby router assumes the active is down and transitions to the active state, taking over the virtual IP and MAC",
                "correct": True,
                "rationale": (
                    "Correct. The HSRP standby router uses the hold timer to detect active failure. "
                    "If hellos are not received within the hold time (default 10s in HSRPv1), "
                    "the standby promotes itself to active, assumes the virtual IP and MAC, "
                    "and sends a gratuitous ARP to update switch MAC tables."
                ),
            },
            {
                "id": "b",
                "text": "The standby sends an alert to the SNMP server but continues waiting",
                "correct": False,
                "rationale": (
                    "Incorrect. SNMP alerts may be configured, but the standby's primary action "
                    "is to promote itself to active, not to passively wait."
                ),
            },
            {
                "id": "c",
                "text": "Both routers independently forward traffic, causing a duplicate IP conflict",
                "correct": False,
                "rationale": (
                    "Incorrect. The standby exclusively takes over the virtual IP; there is no "
                    "scenario where both forward for the virtual IP simultaneously under HSRP design."
                ),
            },
            {
                "id": "d",
                "text": "The virtual IP is suspended until the active router comes back online",
                "correct": False,
                "rationale": (
                    "Incorrect. The whole purpose of the standby is to immediately take over; "
                    "hosts experience only a brief interruption during the hold-timer window."
                ),
            },
        ],
        "explanation": (
            "HSRP failover mechanism: the standby monitors hellos from the active. "
            "Default timers: hello 3s, hold 10s (HSRPv1). If hold expires, standby → active. "
            "The new active sends a gratuitous ARP so switches forward to the new port. "
            "Hosts are unaware; they keep using the same virtual IP/MAC."
        ),
    },
    {
        "id": "cd3v2-036",
        "domain": 3,
        "objective": "3.5",
        "type": "multiple_choice",
        "difficulty": "expert",
        "study_topic": "First hop redundancy (HSRP/VRRP/GLBP)",
        "stem": (
            "In VRRP, can the virtual IP address be the same as one of the router's real interface IP? "
            "What is the consequence?"
        ),
        "options": [
            {
                "id": "a",
                "text": (
                    "Yes — in VRRP, the router whose real IP matches the virtual IP becomes the "
                    "Master automatically regardless of priority; this is called the 'IP Address Owner'"
                ),
                "correct": True,
                "rationale": (
                    "Correct. VRRP allows the virtual IP to equal a real interface IP. "
                    "That router is the 'IP address owner' and automatically has the highest "
                    "effective priority (255), becoming Master unconditionally regardless "
                    "of configured priority values."
                ),
            },
            {
                "id": "b",
                "text": "No — VRRP requires the virtual IP to be a unique address not configured on any real interface",
                "correct": False,
                "rationale": (
                    "Incorrect. VRRP explicitly allows the virtual IP to equal a member's real IP. "
                    "HSRP also supports this, making that router Active/Master."
                ),
            },
            {
                "id": "c",
                "text": "Yes, but the router with the matching IP is always the Backup, not the Master",
                "correct": False,
                "rationale": (
                    "Incorrect. The IP address owner has implicit priority 255 and becomes Master, "
                    "not Backup."
                ),
            },
            {
                "id": "d",
                "text": "It creates a duplicate IP conflict and crashes the VRRP group",
                "correct": False,
                "rationale": (
                    "Incorrect. VRRP is designed to support this scenario; it is a defined "
                    "feature (IP address owner), not an error condition."
                ),
            },
        ],
        "explanation": (
            "VRRP IP address owner: when the virtual IP equals a member's real interface IP, "
            "that router becomes Master with effective priority 255, overriding any configured priority. "
            "This is different from HSRP, which also supports this but calls it the 'active' role. "
            "The IP address owner cannot be preempted in this configuration."
        ),
    },
    {
        "id": "cd3v2-037",
        "domain": 3,
        "objective": "3.5",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "First hop redundancy (HSRP/VRRP/GLBP)",
        "stem": (
            "An engineer configures two HSRP groups on a switch's VLAN interfaces:\n"
            "  SW1: Group 1 active (priority 110), Group 2 standby (priority 90)\n"
            "  SW2: Group 1 standby (priority 90), Group 2 active (priority 110)\n"
            "Half the hosts use Group 1's VIP as their gateway; half use Group 2's VIP. "
            "What is the purpose of this design?"
        ),
        "options": [
            {
                "id": "a",
                "text": "Active/active load sharing — both switches forward traffic simultaneously, each for a different set of hosts",
                "correct": True,
                "rationale": (
                    "Correct. Using multiple HSRP groups with alternating active roles (SW1 active "
                    "for Group 1, SW2 active for Group 2) achieves active/active load sharing. "
                    "Each switch handles real traffic for one group while being standby for the other, "
                    "utilizing both links simultaneously."
                ),
            },
            {
                "id": "b",
                "text": "It is a misconfiguration; HSRP only supports one group per VLAN",
                "correct": False,
                "rationale": (
                    "Incorrect. HSRP supports up to 255 groups (HSRPv1) or 4096 (HSRPv2) per interface; "
                    "multiple groups per VLAN are standard practice for load sharing."
                ),
            },
            {
                "id": "c",
                "text": "Both switches become active for the same group, doubling throughput",
                "correct": False,
                "rationale": (
                    "Incorrect. In each group there is only one active router. SW1 is active for "
                    "Group 1 and SW2 is active for Group 2 — different groups, not both active for the same group."
                ),
            },
            {
                "id": "d",
                "text": "The design ensures only SW1 forwards traffic to minimize asymmetric routing",
                "correct": False,
                "rationale": (
                    "Incorrect. The design deliberately uses BOTH switches for forwarding (load sharing), "
                    "not just one."
                ),
            },
        ],
        "explanation": (
            "Multiple HSRP groups enable active/active load sharing: Group 1 active on SW1, "
            "Group 2 active on SW2. Hosts configured with different VIPs are served by different "
            "active gateways, balancing traffic across both uplinks. "
            "This is a common data-center/campus design for gateway redundancy AND utilization."
        ),
    },
    {
        "id": "cd3v2-038",
        "domain": 3,
        "objective": "3.5",
        "type": "multiple_choice",
        "difficulty": "hard",
        "study_topic": "First hop redundancy (HSRP/VRRP/GLBP)",
        "stem": (
            "An HSRP active router has 'standby 1 preempt' configured with priority 120. "
            "The active router reboots and the standby (priority 100) takes over. "
            "When the original active comes back up, what happens?"
        ),
        "options": [
            {
                "id": "a",
                "text": "The original active (priority 120, preempt enabled) reclaims the active role after a preempt delay",
                "correct": True,
                "rationale": (
                    "Correct. With 'standby preempt', a router with a higher priority than the "
                    "current active can take over the active role upon recovery. The original router "
                    "(priority 120 > current active's 100) reclaims active status, possibly after "
                    "a configurable preempt delay."
                ),
            },
            {
                "id": "b",
                "text": "The standby (priority 100) remains active; HSRP never preempts",
                "correct": False,
                "rationale": (
                    "Incorrect. Without 'preempt', this would be true. But 'standby preempt' is "
                    "explicitly configured, allowing the higher-priority router to reclaim active."
                ),
            },
            {
                "id": "c",
                "text": "Both routers become active simultaneously, causing a gateway conflict",
                "correct": False,
                "rationale": (
                    "Incorrect. HSRP protocol prevents dual-active scenarios; the preemption "
                    "process is coordinated so only one router holds the active role at a time."
                ),
            },
            {
                "id": "d",
                "text": "The router with the lower priority (100) always retains active regardless of preempt",
                "correct": False,
                "rationale": (
                    "Incorrect. 'preempt' overrides the non-preemptive default; the higher-priority "
                    "router (120) takes over when it returns, which is the whole purpose of preempt."
                ),
            },
        ],
        "explanation": (
            "HSRP preempt: by default, HSRP is non-preemptive (a recovered router stays standby). "
            "'standby <group> preempt' allows a router with higher priority to reclaim the active role. "
            "A preempt delay ('standby preempt delay minimum <sec>') prevents flapping during recovery."
        ),
    },
    {
        "id": "cd3v2-039",
        "domain": 3,
        "objective": "3.5",
        "type": "multiple_response",
        "difficulty": "expert",
        "study_topic": "First hop redundancy (HSRP/VRRP/GLBP)",
        "stem": (
            "Which TWO statements about GLBP are true compared to HSRP and VRRP? (Choose TWO.)"
        ),
        "options": [
            {
                "id": "a",
                "text": "GLBP assigns different virtual MAC addresses to different hosts for the same virtual IP, enabling simultaneous active forwarding on multiple routers",
                "correct": True,
                "rationale": (
                    "Correct. The GLBP AVG (Active Virtual Gateway) responds to ARP requests with "
                    "different virtual MAC addresses (up to 4 AVFs), causing different hosts to "
                    "send traffic to different routers — all of which actively forward."
                ),
            },
            {
                "id": "b",
                "text": "GLBP is Cisco-proprietary, whereas VRRP is an open standard",
                "correct": True,
                "rationale": (
                    "Correct. GLBP is Cisco-proprietary (like HSRP). VRRP is an IETF open standard "
                    "(RFC 5798 for VRRPv3). Only VRRP is interoperable across vendors."
                ),
            },
            {
                "id": "c",
                "text": "GLBP uses a single virtual MAC for all hosts, just like HSRP",
                "correct": False,
                "rationale": (
                    "Incorrect. Unlike HSRP (single virtual MAC), GLBP uses multiple virtual MACs "
                    "(one per AVF) to load-balance different hosts to different forwarders."
                ),
            },
            {
                "id": "d",
                "text": "GLBP is an open standard defined by the IETF",
                "correct": False,
                "rationale": (
                    "Incorrect. GLBP is Cisco-proprietary, not an IETF standard. "
                    "VRRP is the IETF equivalent for gateway redundancy."
                ),
            },
        ],
        "explanation": (
            "GLBP (Cisco-proprietary) achieves per-host load balancing with a single VIP via "
            "multiple virtual MACs (one per AVF, up to 4). "
            "HSRP (Cisco) and VRRP (IETF open) are active/standby with a single virtual MAC per group. "
            "GLBP's unique ARP manipulation is the key differentiator."
        ),
    },
    {
        "id": "cd3v2-040",
        "domain": 3,
        "objective": "3.5",
        "type": "multiple_choice",
        "difficulty": "expert",
        "study_topic": "First hop redundancy (HSRP/VRRP/GLBP)",
        "stem": (
            "After an HSRP failover, hosts on the segment still send frames to the original "
            "active router's switch port because the switch's CAM table still maps the virtual "
            "MAC to the old port. What mechanism resolves this?"
        ),
        "options": [
            {
                "id": "a",
                "text": "The new active router sends a gratuitous ARP using the virtual MAC as the source, causing the switch to update its CAM table to the new active's port",
                "correct": True,
                "rationale": (
                    "Correct. After taking over, the new HSRP active sends a gratuitous ARP "
                    "(source IP = virtual IP, source MAC = virtual MAC) from its port. "
                    "The switch sees the virtual MAC arriving on a new port and updates its "
                    "CAM table, redirecting traffic correctly."
                ),
            },
            {
                "id": "b",
                "text": "The switch automatically detects the failover via STP and updates its MAC table",
                "correct": False,
                "rationale": (
                    "Incorrect. STP manages loop prevention and does not have awareness of "
                    "HSRP state. The gratuitous ARP from the new active is what updates the switch."
                ),
            },
            {
                "id": "c",
                "text": "Hosts re-ARP for the gateway IP and receive the new router's real MAC",
                "correct": False,
                "rationale": (
                    "Incorrect. Hosts ARP for the virtual IP and receive the virtual MAC (unchanged). "
                    "The new active's real MAC is irrelevant; it's the virtual MAC that matters, "
                    "and the gratuitous ARP updates the switch's mapping."
                ),
            },
            {
                "id": "d",
                "text": "The CAM table naturally ages out and the switch re-learns the port when traffic arrives",
                "correct": False,
                "rationale": (
                    "Incorrect. While MAC aging would eventually correct this, it takes 300 seconds "
                    "by default. HSRP uses gratuitous ARP specifically to ensure immediate "
                    "CAM table update without waiting for aging."
                ),
            },
        ],
        "explanation": (
            "After HSRP failover, the new active sends a gratuitous ARP (GARP) with: "
            "source IP = virtual IP, source MAC = virtual MAC. Switches update their CAM tables "
            "immediately, redirecting traffic to the new active's port. "
            "Without GARP, traffic would temporarily go to the failed router's port until MAC aging."
        ),
    },
]
